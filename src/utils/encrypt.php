<?php

/**
 * Encripta una cadena de texto.
 * 
 * @param string $raw La cadena de texto a encriptar.
 * @param int $size El tamaño final que tendrá la cadena.
 * 
 * @return string La cadena encriptada.
 */
function encrypt($raw, $size){
   
    return $raw;
}
